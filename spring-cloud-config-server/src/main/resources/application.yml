server:
  port: 8888
  servlet:
    context-path: /config
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: ${user.home}/Projects/configrepo
